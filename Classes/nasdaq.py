from Functions import get_stock_data

class Stocks(object):
    def __init__(self):
        self.storeId = 'nasdaq'

    def update_all(self):
        print('Update all')
        # Update all items in Store.

    def fetch(self, array):
        print(get_stock_data.output(array))
        # check for data in store, if data doesn't exist webscape and add to store.
        


        

        
        
